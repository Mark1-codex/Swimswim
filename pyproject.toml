[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "swimswimproject"
version = "0.1.0"
description = "Simple TUI website DDoS vulenrability tester."
readme = "README.md"
requires-python = ">=3.10"
authors = [
  {name = "MarkKapkan", usernames=[{discord = "ashylittlefemboy", github = "Mark1-Codex"}]}
]

[project.scripts]
yourproject = "main:main"